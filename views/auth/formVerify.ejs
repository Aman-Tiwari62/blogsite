<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogSite</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body{
            height: 95vh;
            width: 100vw;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            position: relative;
        }
        .wrapper{
            display: flex;
            flex-direction: column;
            gap: 2rem;
            padding: 1.5rem;
            box-shadow: 0px 0px 7px rgb(54, 54, 57);
            border-radius: 1rem;
        }
        form{
            margin: 0.8rem;
        }
        .otp{
            width:15rem;
            height: 2.5rem;
            outline: none;
            border: none;
            box-shadow: 0px 0px 3px;
            border-radius: 0.5rem;
            background-color: rgb(238, 238, 238);
            padding: 2px 2.2rem;
            letter-spacing: 12px;
            font-size: 1.5rem;
            
        }
        .btn{
            background-color: rgb(54, 54, 252);
            color: white;
            margin: 0px 1rem;
            padding: 0.3rem 0.5rem;
            border: none;
            border-radius: 0.4rem;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
        }
        h1{
            margin: 0.7rem 0rem;
            font-size: 3rem;
        }
        .logo{
            position: absolute;
            top: 1rem;
            left: 1rem;
            display: flex;
            align-items: center;
            font-size: 2rem;
            color: rgb(41, 72, 85);
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            z-index: 10;
        }
        .logo>img{
            width: 4rem;
        }
        .resend:disabled {
            background-color: #817a7a;
            cursor: not-allowed;
            opacity: 0.6;
        }
        p{
            font-size: 1.2rem;
            color: rgb(68, 68, 68);
        }
        .resendbox{
            display: flex;
            align-items: center;
            gap: 2rem;
        }
        a{
            color: white;
            text-decoration: none;
        }
        .success-wrapper{
            position: absolute;
            height: 100%;
            width: 100%;
            top: 0px;
            left: 0px;
            /* filter: blur(8px); */
            display: none;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(10px); /* blur background behind parent */
            -webkit-backdrop-filter: blur(10px); /* for Safari */
        }
        .success{
            background-color: white;
            border: 4px solid gray;
            border-radius: 0.7rem;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 2rem;
            transition: transform 0.1s ease-in;
            transform: scale(0);
        }

    </style>
</head>
<body>
    <a class="logo" href="/">
        <img src="../images/icon.png" alt="">
        <span>BlogSite</span>
    </a>
    <div class="success-wrapper">
        <div class="success">
            <p>Registration Successfull !!</p>
            <button class="btn continue">Continue</button>
        </div>
    </div>
    <div class="wrapper">
        <div class="box1">
            <h1>Verify your email:</h1>
            <p>An OTP has been sent to your regsitered email: <%=email%> </p>
            <p class="otp-expire-message">(OTP expires in <span class="expire-time"></span> seconds )</p>
        </div>
        <form >
            <input class="otp" type="text" maxlength="6" name="otp">
            <input class="btn verify" type="submit" value="Verify">
        </form>
        <div class="resendbox">
            <button class="btn resend">Resend</button>
            <p class="message">Resend OTP in <span class="timer"></span> seconds</p>
        </div>
        <p class="error-message" style="visibility: hidden; color: red;">here goes the error message</p>
    </div>

    <script src="../js/verify.js"></script>

</body>
</html>